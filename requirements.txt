# Runtime deps for the live demo (src/ui/app.py), pinned to the versions in uv.lock.
# gradio itself is intentionally omitted: on Hugging Face Spaces the `sdk_version`
# in README.md controls the installed gradio version.
mediapipe==0.10.35
numpy==2.5.1
opencv-python==5.0.0.93
scikit-learn==1.9.0
scipy==1.18.0
matplotlib==3.11.1
joblib==1.5.3
